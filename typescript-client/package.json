{
  "scripts": {
    "build": "tsc -b",
    "postbuild": "cp package.json dist/package.json && cd dist && npm install --only=production && cd .. && npx browserify dist/main.js -o public/index.js",
    "watch": "tsc -w"
  },
  "devDependencies": {
    "@types/jquery": "^3.5.31",
    "browserify": "^17.0.1",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "jquery": "^3.7.1"
  }
}
